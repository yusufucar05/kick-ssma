import 'package:flutter/material.dart';

class ConnectAccountPage extends StatelessWidget {
  const ConnectAccountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Buraya ileride Kick WebView gelecek"),
    );
  }
}

